import type { CardType } from '../type'

export const useHardMode = () => {
  const firstUserTurnHardMode = (firstCard: CardType) => {
    return
  }

  const secondUserTurnHardMode = async (secondCard: CardType) => {
    return
  }

  const cpuTurnHardMode = async () => {
    return
  }
  return { firstUserTurnHardMode, secondUserTurnHardMode, cpuTurnHardMode }
}
